export enum CardShape {
  ROUND = 'round',
  SQUARE = 'square',
}

export enum CardAlingnment {
  LEFT = 'left',
  CENTER = 'center',
}

export enum ImageTransformationMode {
  NO_TRANSFORMATION = 'noTransformation',
  CENTER_CROP = 'centerCrop',
}

export const REQUEST_FOCUS_ACTION = 'request-focus';
