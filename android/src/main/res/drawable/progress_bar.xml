<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background" android:drawable="@color/progress_bg"/>
    <item android:id="@android:id/progress">
        <clip>
            <color android:color="@color/progress_primary" />
        </clip>
    </item>
</layer-list>